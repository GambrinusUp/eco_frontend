let initialState = {

}

const authenticationReducer = (state = initialState, action) => {
    let newState = { ...state };
    switch (action.type) {
        default:
            return state;
    }
}

export default authenticationReducer;
